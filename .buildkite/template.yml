name: "Kubernetes example pipeline"
steps:
  - label: ":pipeline: Upload pipeline definition"
    command: "buildkite-agent pipeline upload .buildkite/pipeline.yml"
    agents:
      queue: kubernetes
